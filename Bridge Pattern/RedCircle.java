public class RedCircle implements IDrawAPI{
    
    public void drawCircle(int radius , int x , int y){
        System.out.println("Drawing Red Circle with[radius = "+ radius + ", Origin = ("+ x + "," + y + ")]");
    }
}
